Use 'bd' for task tracking

Path scope rule (all projects):
- Always read/write plans, notes, and generated artifacts inside the current repository.
- Never use global paths like ~/.cursor/plans for project work.

OpenCLI preference:
- Prefer `opencli` for website automation, supported desktop app control, and passthrough to existing CLIs.
- Start with `opencli list` and `opencli <adapter> --help` before inventing command syntax.
- Run `opencli doctor` for any Chrome-backed or logged-in browser workflow.
